package com.github.vaerys.test;

import static org.junit.Assert.fail;
import java.util.Arrays;
import java.util.List;
import org.junit.jupiter.api.Test;
import com.github.vaerys.commands.CommandInit;
import com.github.vaerys.templates.Command;

class Test_Commands {

//    @Test
//    public void NoCommandNameAreTheSame() {
//        List<Command> commands = CommandInit.get();
//        for(Command c : commands) {
//            String[] names = c.names;
//            for (String name : names) {
//                if (commands.stream().filter(command -> Arrays.asList(command.names).contains(name) && command != c ).count() > 0) {
//                    fail(String.format("The command name %s is repeated twice",name));
//                }
//            }
//        }
//    }

}
